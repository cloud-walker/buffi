{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter buffi run prepare && pnpm --filter buffi run build",
    "watchPatterns": ["apps/app/**", "pnpm-lock.yaml", "pnpm-workspace.yaml"]
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
